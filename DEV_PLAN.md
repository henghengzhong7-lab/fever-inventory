# FEver 战队物资管理 —— 开发计划（v1.0）

| 项目 | 内容 |
| --- | --- |
| 依据文档 | PRD.md v1.0（2026-09-21） |
| 编写日期 | 2026-09-21 |
| 目标版本 | 第一版：电脑网页版、纯本地离线、数据存本机 |
| 计划性质 | 按里程碑推进，每个里程碑有明确交付物、涉及文件、验收对照与通过条件 |
| 本机环境 | Chrome（C:\Program Files\Google\Chrome\Application\chrome.exe）、Edge、Node v24.19.0 |

---

## 1. 计划总览

第一版拆成 9 个里程碑（M0-M8），每个里程碑都是一个"能跑起来、能看到东西"的状态，不出现"写了三天看不到效果"的阶段。

| 里程碑 | 名称 | 交付物（能看见的结果） | 对照验收 | 预估 |
| --- | --- | --- | --- | --- |
| M0 | 地基与打开方式实测 | 确定最终打开方式；空壳页面能在离线状态打开；IndexedDB 能建库并读写成功 | AC-01 / AC-35 | 0.5 次会话 |
| M1 | 数据层与编码规则 | 表结构落地；编码 MC-0001 顺延；备份导出、导入恢复可用；npm test 全绿 | AC-09 / AC-31 / AC-32 | 1 次会话 |
| M2 | 界面框架与首页 | 顶部导航、页面切换、首页四大类卡片（带真实统计数字） | AC-05 | 1 次会话 |
| M3 | 物品身份、二维码与标签 | 两种身份模式入库；物品详情页；二维码显示；标签打印页 | AC-07 / AC-08 / AC-10 / AC-11 | 1-1.5 次会话 |
| M4 | 采购、到货入库与发票 | 采购申请全流程；确认到货自动入库；发票台账；三者互跳 | AC-12 ~ AC-16 | 1-1.5 次会话 |
| M5 | 出入库操作台、流水与借用台账 | 借出/归还/领用/送修/修好；流水筛选；借用台账与超期高亮 | AC-17 ~ AC-27 | 1.5 次会话 |
| M6 | 四大类专属页与提醒 | 四类页面功能各不相同；低库存/标定/校准/易损件提醒 | AC-06 / AC-28 ~ AC-30 | 1.5 次会话 |
| M7 | 设置、备份提醒与启动固化 | 设置页；导出导入入口；备份提醒；启动脚本或双击说明 | AC-33 / AC-34 | 0.5 次会话 |
| M8 | 全量验收与收尾 | 按 AC-01 ~ AC-35 逐条自查通过；README 使用说明 | 全部 | 1 次会话 |

执行顺序原则上不可跳：M1 是所有功能的地基，M3 的编码/身份是 M4、M5 的前置。M6 依赖 M3/M5 的字段与状态。

---

## 2. 必须先定下来的技术决策

### 2.1 打开方式（M0 实测后二选一，写进 README）

PRD 7.2 节要求二选一。本地文件方式（file://）与本地服务方式（http://localhost）会产生**两套互不相通的数据**，必须只留一种。

- 方案 A：双击 index.html（file://）——零依赖，但要实测 Chrome 在该模式下 IndexedDB 是否可用、数据是否留存。
- 方案 B：双击启动.bat 起本地 Node 静态服务并自动开浏览器——完全离线，存储最稳。

**M0 实测步骤（可直接执行）**
1. 在应用目录放一个最小测试页，写入一条记录到 IndexedDB，读回并显示。
2. 分别用 file:// 双击方式和 http://localhost 方式打开，各测一遍。
3. 关闭浏览器重开，确认数据仍在。
4. 记录结论：选哪一种，以及为什么。若方案 A 通过则优先 A（对使用者最简单）。

### 2.2 代码组织方式（受打开方式约束）

file:// 模式下浏览器会拦截 ES Module 加载（CORS 限制）。因此**统一采用普通 script 标签 + IIFE 命名空间**写法，不用 import/export。这样无论最终选 A 还是 B，代码都不用改。

### 2.3 目录归属（需在 M0 一并确认）

旧版本文件（index.html、js/、css/、test/）与新版目录规划重名。PRD 11 节写的是根目录规划，但旧文件不能删。两种处理方式：

- 推荐：把旧版本整体移到一个子目录（例如 old-v0\）保留，新版占用根目录，使用者双击的就是根目录的 index.html，最不容易搞混。移动旧文件前会征得同意。
- 备选：旧文件原地不动，新版放 v1\ 子目录。完全不动旧文件，但使用者要记得点进 v1。

**M0 结束时确认一种，并同步更新 PRD 11 节。**

### 2.4 可复用的现成资源

旧目录里已有一份本地二维码库 js\lib\qrcode.min.js（约 20KB，无需联网）。新版直接复制使用，不重新下载。同目录的 html5-qrcode.min.js 是摄像头扫码用的，第一版不需要，第二版再启用。

---

## 3. 目录结构（新版目标状态）

```
FEver战队物资管理\
├─ index.html            页面骨架（单页多视图）
├─ 启动.bat              仅当选方案 B 时需要
├─ README.md             怎么打开、怎么备份、常见问题
├─ PRD.md                需求依据
├─ DEV_PLAN.md           本文件
├─ css\
│   └─ style.css         全局样式（含打印样式 @media print）
├─ js\
│   ├─ db.js             数据层：建库、表结构、增删改查、备份导出导入
│   ├─ rules.js          业务规则：编码生成、状态流转、库存自洽、提醒判定
│   ├─ ui.js             通用界面件：表格、弹窗、提示、确认、筛选栏
│   ├─ app.js            页面路由与各页面渲染、事件绑定
│   ├─ modules\
│   │   ├─ mechanical.js 机械专属：装配位置检索、标准件库存、安全库存
│   │   ├─ electronic.js 电控专属：安全警示、易损件周期、电池健康
│   │   ├─ vision.js     视觉专属：一件一码、镜头配套、标定提醒
│   │   └─ hardware.js   硬件专属：工具借还、耗材余量、校准到期
│   └─ lib\
│       └─ qrcode.min.js 本地二维码生成库（沿用旧目录的文件）
└─ test\
    └─ test.js           Node + fake-indexeddb 自动测试（npm test）
```

---

## 4. 数据模型（M1 落地，后续所有功能依赖它）

数据库名 FEVER_INV_V1，版本号 1。全部存 IndexedDB，不使用任何本地文件存储数据（备份文件除外）。

### 4.1 表一览

| 表名 | 主键 | 作用 | 关键索引 |
| --- | --- | --- | --- |
| categories | id（字符串） | 四个大类配置与专属字段定义 | name |
| items | code（字符串，即物品编码） | 物品身份（一件一码或同款共用） | categoryId、status、purchaseRequestId、updatedAt |
| lots | id（自增） | 同款共用身份的库存账（按"款"聚合） | itemCode、categoryId |
| purchaseRequests | id（自增） | 采购申请 | categoryId、status、createdAt |
| invoices | id（自增） | 发票 | purchaseRequestId、invoiceNo |
| transactions | id（自增） | 出入库流水（只追加，不修改不删除） | itemCode、type、operator、createdAt |
| settings | key（字符串） | 零散配置：上次备份时间、导出格式版本等 | 无 |

### 4.2 items（物品身份）

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| code | string | 是 | 编码，主键，格式 MC-0001，生成后不可改 |
| name | string | 是 | 名称 |
| spec | string | 否 | 规格 / 型号 |
| categoryId | string | 是 | mechanical / electronic / vision / hardware |
| location | string | 否 | 存放位置 |
| identityMode | string | 是 | single（单独建身份）或 shared（同款共用） |
| totalQty | number | 是 | 总件数；single 恒为 1 |
| inStockQty | number | 是 | 在库件数 |
| lentQty | number | 是 | 借出件数 |
| repairQty | number | 是 | 待修件数 |
| usedUpQty | number | 是 | 累计领用件数（仅 shared 有意义） |
| status | string | 是 | in_stock / lent / repairing / used_up（派生展示用，见 4.6） |
| safetyStock | number | 否 | 安全库存，低于此值提醒补货 |
| remark | string | 否 | 备注 |
| extra | object | 否 | 大类专属字段键值对 |
| purchaseRequestId | number | 否 | 来源采购申请 |
| invoiceId | number | 否 | 来源发票 |
| createdAt | string | 是 | 入库时间（ISO） |
| updatedAt | string | 是 | 最后变动时间（ISO） |

### 4.3 transactions（流水，只追加）

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | number | 是（自增） | 主键 |
| itemCode | string | 是 | 物品编码 |
| type | string | 是 | inbound / lend / return / consume / repair / repair_done |
| qty | number | 是 | 本次涉及件数（single 为 1） |
| operator | string | 是 | 操作人 |
| borrower | string | 否 | 借用人（lend 时必填） |
| dueDate | string | 否 | 预计归还日期（lend 时必填） |
| purpose | string | 否 | 用途 / 领用原因 / 故障说明 |
| fromStatus | string | 否 | 操作前状态 |
| toStatus | string | 否 | 操作后状态 |
| snapshotInStock | number | 否 | 操作后在库件数（便于回溯） |
| createdAt | string | 是 | 发生时间（ISO） |

### 4.4 purchaseRequests 与 invoices

| purchaseRequests 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | number（自增） | 主键 |
| categoryId | string | 所属大类 |
| name / spec | string | 物品名称、规格 |
| quantity | number | 申请数量 |
| budget | number | 预算 |
| purpose | string | 用途 |
| applicant | string | 申请人 |
| status | string | pending（待购买）/ ordered（已下单）/ arrived（已到货）/ canceled（已取消） |
| createdAt / updatedAt | string | 时间 |

| invoices 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | number（自增） | 主键 |
| purchaseRequestId | number | 来源申请 |
| invoiceNo | string | 发票号（必填） |
| amount | number | 金额（必填） |
| supplier | string | 供应商（必填） |
| invoiceDate | string | 发票日期 |
| createdAt | string | 登记时间 |

### 4.5 关联字段与跳转

- items.purchaseRequestId -> purchaseRequests.id；items.invoiceId -> invoices.id。
- invoices.purchaseRequestId -> purchaseRequests.id。
- transactions.itemCode -> items.code。
- 跳转实现：所有关联处用 data-goto 属性挂"表名:id"，由 app.js 统一处理点击跳转，不用内联 onclick 拼字符串。
- 断链处理：查不到上游时显示"来源已删除"，不报错、不阻断（对照 AC-27）。

### 4.6 状态与库存自洽规则（写在 rules.js）

- 恒等式：inStockQty + lentQty + repairQty + usedUpQty = totalQty（对 shared）；single 恒为 1（在库或借出或待修）。
- status 由数量派生：全部在库 -> in_stock；有借出 -> lent；有待修 -> repairing；全部领用完毕 -> used_up。
- 任一操作后必须校验恒等式，不成立则拒绝提交并提示（对照 AC-24）。
- 流水只追加：不提供删除流水的入口（对照第 2 节名词定义）。

---

## 5. 里程碑详细计划

### M0 地基与打开方式实测

| 项 | 内容 |
| --- | --- |
| 目标 | 确定打开方式；证明"关掉浏览器数据还在"；定目录归属 |
| 涉及文件 | 新建 test-open.html（临时）、可能新建 启动.bat |
| 工作项 | 1) 建最小 IndexedDB 读写测试页；2) Chrome 下分别用 file:// 与 localhost 各测一遍；3) 关浏览器重开验证数据留存；4) 选定方案并记录理由；5) 确认目录归属（2.3 节）；6) 复制旧 qrcode.min.js 到新版 js\lib |
| 交付物 | 打开方式结论 + 可双击打开的空壳页面 |
| 验收对照 | AC-01（离线加载无报错）、AC-35（无联网） |
| 通过条件 | 双击打开无控制台报错；写入的记录在重开浏览器后仍能读到 |
| 风险 | 若 file:// 下 IndexedDB 被限制（部分浏览器对本地文件存储较严格），直接切方案 B，不停留 |

### M1 数据层与编码规则

| 项 | 内容 |
| --- | --- |
| 目标 | 数据能存、能读、能备份、能恢复，编码按大类顺延 |
| 涉及文件 | js\db.js、js\rules.js、test\test.js、package.json |
| 工作项 | 1) 按第 4 节建全部表与索引；2) 实现 openDB / add / put / get / getAll / getByIndex / delete 封装；3) 实现编码生成：按大类取当前最大流水号 +1，补零到四位；4) 实现状态与库存自洽校验；5) 实现备份导出（含 formatVersion、exportedAt、各表数据）与导入恢复；6) 初始化四个大类的默认配置（含专属字段定义）；7) 写 npm test 覆盖：建库、四类初始化、编码顺延不重复、库存恒等式、导出导入往返一致 |
| 交付物 | npm test 全部通过；控制台可手工跑通一次导出/导入往返 |
| 验收对照 | AC-09、AC-31、AC-32（逻辑层面） |
| 通过条件 | 测试全绿；同一大类连续生成编码得到 MC-0001、MC-0002；导出后清空再导入，各表条数一致 |
| 风险 | 编码生成要考虑"同款共用"与"单独身份"批量生成（一次入 5 件要生成 5 个连续编码且不重号），测试要覆盖 |

### M2 界面框架与首页

| 项 | 内容 |
| --- | --- |
| 目标 | 有导航、能切页、首页能看到四个大类与真实统计 |
| 涉及文件 | index.html、css\style.css、js\ui.js、js\app.js |
| 工作项 | 1) 顶部固定导航（首页 / 采购 / 发票 / 流水 / 借用 / 设置）+ 面包屑 + 返回；2) 单页多视图路由（hash 或 data-view 切换）；3) 首页四张卡片：数量、在借、待修、低库存、待购买；4) 首页最近动态（取流水最近 10 条）；5) 快捷操作区（直接入库、新建采购申请、出入库操作台）；6) ui.js 提供表格、弹窗、确认、轻提示、筛选栏通用件 |
| 交付物 | 首页可见，点卡片能进对应大类页（页面先占位） |
| 验收对照 | AC-05 |
| 通过条件 | 首页只有机械/电控/视觉/硬件四张卡片，数字与库内数据一致；导航切换无报错 |
| 风险 | 统计必须实时算，不能缓存，避免与明细不一致（PRD 5 节约定） |

### M3 物品身份、二维码与标签

| 项 | 内容 |
| --- | --- |
| 目标 | 能建身份、看到二维码、打出标签 |
| 涉及文件 | js\app.js、js\rules.js、js\modules\*、js\lib\qrcode.min.js、css（打印样式） |
| 工作项 | 1) 新增物品表单：通用字段 + 按大类动态渲染专属字段；2) 身份模式开关（单独建身份 / 同款共用），视觉类默认单独；3) 批量生成连续编码；4) 物品列表与详情页（基本信息、专属字段、状态、二维码、履历时间线、关联跳转）；5) 二维码内容 FEVER:ITEM:编码，本地生成；6) 标签打印页：单张 + 批量勾选，版式含二维码与文字，@media print 隐藏导航；7) 搜索与筛选（名称 / 编码 / 规格 / 位置） |
| 交付物 | 完整的物品档案与标签打印能力 |
| 验收对照 | AC-07、AC-08、AC-10、AC-11 |
| 通过条件 | 1 件单独身份生成 1 码；5 件共用身份生成 1 码且件数 5；扫出的二维码文本为 FEVER:ITEM:编码；打印预览无导航栏且二维码清晰 |
| 风险 | 打印版式在不同纸张下会跑版，先在浏览器打印预览里校对 A4 与常见不干胶尺寸 |

### M4 采购、到货入库与发票

| 项 | 内容 |
| --- | --- |
| 目标 | 从提申请到自动入库、发票可查全流程打通 |
| 涉及文件 | js\app.js、js\rules.js（入库逻辑） |
| 工作项 | 1) 采购申请列表 + 新建表单（名称、规格、数量、预算、用途、申请人、大类）；2) 状态流转：待购买 -> 已下单 -> 已到货，可取消；3) 确认到货弹窗：到货数量、存放位置、发票号、金额、供应商、日期、"单独建身份"开关（按大类预勾选）；4) 提交事务：生成物品身份并入库 + 写发票 + 写 inbound 流水 + 标记申请已到货，**要么全成要么全不成**；5) 发票台账与详情，可跳回申请与生成的物品；6) 物品详情里显示来源采购与发票 |
| 交付物 | 采购全链路可用，物品自动出现在对应大类 |
| 验收对照 | AC-12 ~ AC-16 |
| 通过条件 | 到货提交后物品状态为在库且能追到申请与发票；发票必填项留空无法提交且有提示；三处互跳都能到 |
| 风险 | 事务性：IndexedDB 单事务跨 4 张表写入，若中途失败要能整体回滚，测试需覆盖失败分支 |

### M5 出入库操作台、流水与借用台账

| 项 | 内容 |
| --- | --- |
| 目标 | 所有状态类操作有统一入口，流水与借用台账准确 |
| 涉及文件 | js\app.js、js\rules.js |
| 工作项 | 1) 操作台：输入编码识别物品 -> 显示当前状态 -> 选动作（借出/归还/领用/送修/修好回库）-> 填表 -> 展示"本次变化" -> 提交；2) 借出需借用人 + 用途 + 预计归还日期；归还清理借出状态；领用扣库存；送修与修好回库；3) 流水页：按物品 / 操作人 / 类型 / 时间范围筛选；4) 借用台账：从流水推导的在借清单、超期高亮"超期 N 天"、一键归还；5) 支持 USB 扫码枪（等同键盘输入，输入框回车即识别）；6) 编码不存在时提示"未找到该物品"且不写记录 |
| 交付物 | 出入库全流程可用，借用情况一目了然 |
| 验收对照 | AC-17 ~ AC-27 |
| 通过条件 | 借出后状态与台账一致；归还后台账消失且流水 +1；昨天的归还日期显示超期 1 天；领用 3 件后件数正确减少；库存恒等式始终成立 |
| 风险 | 同款共用物品的"部分借出/部分领用"最容易出错，规则集中在 rules.js 并有测试覆盖 |

### M6 四大类专属页与提醒

| 项 | 内容 |
| --- | --- |
| 目标 | 四个大类功能各不相同，提醒可用 |
| 涉及文件 | js\modules\mechanical.js、electronic.js、vision.js、hardware.js、js\rules.js |
| 工作项 | 1) 机械：按装配位置检索、标准件库存视图、安全库存高亮；2) 电控：安全等级警示样式、易损件更换周期到期、电池循环次数与健康；3) 视觉：标定状态与日期、超期"需重新标定"、镜头与相机配套关系、贵重超期重点提醒；4) 硬件：工具校准/检定到期、耗材余量、低库存报警；5) 各类页面顶部展示本类专属统计与提醒条 |
| 交付物 | 四类页面明显不同，各有专属能力 |
| 验收对照 | AC-06、AC-28 ~ AC-30 |
| 通过条件 | 新增物品时只出现本类专属字段；低于安全库存、标定超期、校准到期都能在对应页面出现提示 |
| 风险 | "标定周期""更换周期"需要一个默认值（例如标定 90 天、校准 180 天），放在 categories 配置里可改，避免写死 |

### M7 设置、备份提醒与启动固化

| 项 | 内容 |
| --- | --- |
| 目标 | 使用者能自助备份恢复，启动方式最终定稿 |
| 涉及文件 | js\app.js、启动.bat（若选方案 B）、README.md |
| 工作项 | 1) 设置页：导出备份、导入恢复、上次备份时间、数据概况（各表条数）；2) 导入前自动导出当前数据为保险 + 二次确认（对照 AC-33）；3) 超过 7 天未备份提示（对照 AC-34）；4) 固化启动方式并写 README：怎么打开、怎么备份、怎么恢复、数据在哪、什么情况下会丢 |
| 交付物 | 完整可用 + 有说明书 |
| 验收对照 | AC-33、AC-34 |
| 通过条件 | 取消导入时不发生任何改变；导入后各表条数与文件一致；README 步骤照做能跑通 |

### M8 全量验收与收尾

| 项 | 内容 |
| --- | --- |
| 目标 | AC-01 ~ AC-35 逐条通过 |
| 工作项 | 1) 按验收清单逐条实测并打勾，结果记入 README 或单独验收记录；2) npm test 全绿；3) 清理临时文件（如 M0 的测试页）；4) 检查全站无联网请求、无登录、无摄像头调用、无手机端元素（AC-35）；5) 数据持久化三项测试：F5 刷新、关浏览器重开、重启电脑 |
| 交付物 | 验收通过的成品 + 验收记录 |
| 通过条件 | 35 条全部通过；未通过项必须写明原因与是否可延后 |
| 风险 | 重启电脑一项需要使用者配合执行，提前说明 |

---

## 6. 测试策略

### 6.1 自动测试（Node + fake-indexeddb，npm test）

覆盖所有不依赖界面的逻辑，每次改动都要跑。

| 测试组 | 覆盖点 | 对照验收 |
| --- | --- | --- |
| 建库与初始化 | 表与索引建全、四个大类默认配置写入 | AC-05 前置 |
| 编码规则 | MC-0001 起顺延、不复用、批量生成连续不重号、四类前缀正确 | AC-09 |
| 库存恒等式 | 在库 + 借出 + 待修 + 已领用 = 总数；single 恒为 1 | AC-24 |
| 状态流转 | 借出/归还/领用/送修/修好回库后状态与数量正确 | AC-19 ~ AC-23 |
| 借用超期 | 昨天到期判定为超期 1 天 | AC-21 |
| 提醒判定 | 低安全库存、标定超期、校准到期、易损件到期 | AC-28 ~ AC-30 |
| 到货入库事务 | 成功路径生成物品+发票+流水；失败路径整体回滚不留脏数据 | AC-14、AC-15 |
| 关联与断链 | 上游删除后下游显示"来源已删除"且不报错 | AC-27 |
| 备份往返 | 导出后清空再导入，各表条数与内容一致 | AC-31、AC-32 |
| 必填校验 | 发票号/供应商/物品名称等留空被拒 | AC-16 |

### 6.2 人工测试（浏览器实点）

界面、打印、持久化这些必须人工验，M0/M2/M3/M5/M7/M8 各阶段收尾时各做一轮。

- 打印：Chrome 打印预览校对标签版式，实打一张确认二维码可扫（对照 AC-10）。
- 持久化三连测：F5 刷新、关浏览器重开、重启电脑（对照 AC-02 ~ AC-04）。
- 离线验证：断网状态下打开并完成一次完整操作（对照 AC-01、AC-35）。
- 扫码枪验证：若手边有 USB 扫码枪，插上试一次回车识别（对照 AC-17）。

---

## 7. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| file:// 下 IndexedDB 受限制 | 数据存不住，是最严重风险 | M0 先实测，不通过立即切方案 B（本地静态服务） |
| 两种打开方式混用 | 出现两套数据，使用者以为数据丢了 | M0 定死一种，README 写明，启动文件只保留一个 |
| 清浏览器数据导致丢失 | 数据不可恢复 | 备份提醒 + README 强调每周导出，备份文件存网盘/U 盘 |
| 到货入库跨表写入中断 | 出现半成品数据 | 单事务提交 + 失败整体回滚 + 自动测试覆盖失败分支 |
| 同款物品部分借出/部分领用 | 库存数字错乱 | 规则集中在 rules.js，恒等式在每次提交前校验 |
| 打印版式跑版 | 标签不可用 | 先在打印预览校对多种纸张尺寸，再实打验证 |
| 编码批量生成重号 | 标签冲突 | 生成时在事务内读取并锁定最大流水号，测试覆盖并发场景 |
| 需求追加导致范围膨胀 | 拖慢交付 | 严格按 PRD 第 9 节"不做清单"执行，新想法记入第二版待议 |

---

## 8. 建议执行顺序与节奏

1. **第一批（地基，必须先做）**：M0 + M1。这两步做完，"能不能存住数据"和"编码规则对不对"就有了确定答案，后面才敢往上堆功能。
2. **第二批（骨架与档案）**：M2 + M3。做完就能开始录真实物品、打印标签贴到东西上，实用性马上体现。
3. **第三批（业务闭环）**：M4 + M5。做完采购、出入库、借用就全通了，这是核心价值部分。
4. **第四批（差异化与收尾）**：M6 + M7 + M8。四大类专属能力、设置备份、全量验收。

每个里程碑结束时我会：跑一遍 npm test，按对应验收条目自查，把结论和下一步告诉你，确认无误再进入下一个里程碑。

---

## 9. 开发完成定义（Definition of Done）

一个里程碑算完成，需同时满足：

- 功能按本节工作项全部实现，无占位残留。
- 对照的 AC 条目逐条实测通过，有记录。
- npm test 全绿，新增逻辑有对应测试。
- 无控制台报错、无联网请求。
- 数据操作后库存恒等式成立。
- 相关文件路径与目录结构符合第 3 节，旧文件保持不动。

---

## 10. 计划变更记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v1.0 | 2026-09-21 | 依据 PRD v1.0 编写，拆分为 M0-M8 九个里程碑 |
