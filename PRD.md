# FEver 战队物资管理 —— 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v3.0 |
| 更新日期 | 2026-09-22 |
| 文档状态 | 与需求方逐轮确认开发完成，全部功能已上线并验收 |
| 线上地址 | https://fever-inventory.app.workbuddy.host/ （飞书内打开，免登） |

> 本文档描述系统的**当前完整形态**。历史上的 v1.0（纯离线版需求，808 行）与 v2.0（飞书共享版方案）
> 完整保留在 git 历史中；面向用户的部署操作手册在 `docs/FEISHU_SETUP.md`。

---

## 1. 产品概述

### 1.1 一句话描述

机器人战队 FEver 的物资管理系统：物品有唯一身份和二维码，采购申请经管理员审批后到货自动入库并留存发票 PDF，出入库全程留痕可追溯，按兵种管理预算，飞书消息实时提醒审批流转。

### 1.2 两套运行形态（同一份界面代码）

| | 本地离线版 | 飞书共享版（主力） |
| --- | --- | --- |
| 入口 | `启动物资管理.bat`（本机 8321 端口） | 飞书工作台 / 浏览器打开线上地址 |
| 数据 | 本机浏览器 IndexedDB | 飞书多维表格（全队共享一份数据） |
| 登录 | 无 | 飞书免登，服务端签发会话令牌 |
| 用途 | 现场断网时的兜底 | 日常使用 |

**硬约束：两套形态共用同一份界面代码**（`v1/` 目录，构建时原样复制给飞书版，一行不加工）；
离线版 `v1/` 不允许出现任何联网调用（有专门测试逐文件检查）。

### 1.3 角色与权限

- **管理员**（`server/config.json` 的 `admins`，姓名或飞书 openId 混写）：审批/驳回采购申请、删除数据、批量下载发票、设置兵种预算。
- **普通队员**：提交采购申请、入库、借还/维修登记、查看各类台账、提交自己的申请后等飞书通知。
- 权限是**双层**的：前端 `isAdmin()` 只决定按钮画不画；服务端 `applyOps` 强制校验，改前端绕不过去。

---

## 2. 功能清单

### 2.1 物品与四大类

- 四个大类（机械 / 电控 / 视觉 / 硬件）各有专属页面与专属字段。
- 物品两种身份模式：**单独建身份**（一件一个编码，如 `VS-0001`）/ **同款共用**（N 件共用一个编码，按数量记账）。
- 编码自动生成并全局唯一（撞号整批 409 拒绝，前端重拉数据后重试）。
- 物品状态机：在库 / 借出 / 待修 / 已领用，库存恒满足 `在库 + 借出 + 待修 + 已领用 = 总数`。
- 二维码标签打印：单个、批量勾选、按大类、全部四种入口，版式统一。
- 物品详情页可回溯：来源采购申请 → 来源发票 → 生成的物品，三者互相跳转。

### 2.2 采购申请与审批

- 队员提交采购申请：**必填兵种**（重装/步兵/哨兵/飞镖/无人机/雷达/前哨战/能量机关/其他）、物品名称、数量；预算金额选填。
- 状态流转：待审批 → 已同意 →（标记）已下单 → 确认到货；驳回（必填理由）与取消为终态。
- **管理员同意的那一刻才占用预算**；驳回/取消不计。
- **飞书提醒**：提交 → 私聊通知每位管理员；批准 → 私聊通知申请人。fire-and-forget，发送失败绝不阻塞业务。
- 申请人身份（openId）在提交时记进申请行，批准提醒优先按它送达。

### 2.3 到货入库与发票

- 确认到货**不填发票号、不填供应商**，改为上传**发票 PDF**（≤20MB，只认 PDF）。
- PDF 支持**桌面拖拽**与点选两种入口。
- 发票号码选填：不填时以文件名回退显示（`invoiceLabel`：发票号 → 文件名 → 发票 #id）。
- 存储：离线版 base64 进数据 JSON；飞书版传服务器 `server/uploads/`，行里只留 `fileRef` 引用（两版数据层接口一致）。
- 不传 PDF 也能完成到货入库。

### 2.4 发票管理与批量下载

- 发票台账：全部发票、合计金额、来源申请、生成物品。
- 发票详情弹窗可单张下载 PDF。
- **批量下载（可勾选）**：管理员点「批量下载发票（N）」弹出选择列表——默认全选、全选/全不选/自由勾选、实时计数「已选 k / N 张」；一张不选时下载按钮禁用。确认后把勾选的发票打包成 ZIP（重名自动加序号）。
- ZIP 由自研打包器生成（store 模式、CRC32、UTF-8 文件名、无第三方依赖——离线版不能引 CDN）。

### 2.5 出入库与借用

- 入库 / 借出 / 归还 / 领用 / 维修登记，全部走同一套流水。
- **流水只追加、不修改**；删除开明确例外：每删一条在删除记录里留痕（谁删的、何时、完整快照），界面**刻意不提供"清空删除记录"**。
- 借用台账显示未归还与超期（超期高亮告警）。
- 批量删除物品：勾选后整批一个事务；借出/待修的**跳过**而非整批失败；删除与留痕同生共死。

### 2.6 兵种预算

- 管理员按兵种设置预算额度（存设置表，不动表结构）。
- 汇总页每兵种一行：预算 / 已用（已同意）/ 剩余 / 使用率 / 待审批金额 / 已到货实际 / 申请数；支持 全部 / 本年 / 本季 / 本月 四种时间口径。
- 「未指定」兵种（加兵种字段前的老申请）单独成行，照样计入总额——保证明细加起来等于总数。
- **预算导出**（CSV，Excel 可直接打开），三段式：
  1. 兵种预算总表（每兵种一行 + 消费情况标记：超支 / 已消费 / 未消费 / 无预算记录）；
  2. 【已消费明细】逐条已同意申请（兵种、编号、名称、申请人、数量、申请金额、到货实际取发票真实金额、状态）；
  3. 【未消费预算】仍有剩余额度的兵种及余额。
- 导出固定"全部时间"全量口径（存档要完整账，不跟页面筛选走）。

### 2.7 扫码查物（手机端）

- 四级降级：飞书原生扫码（JSAPI 鉴权）→ 摄像头实时识别（jsQR）→ 拍照/选图 → 手工输入。
- 扫码直达物品结果页，可直接在手机上登记借用，电脑端实时同步。
- 「拍照 / 选图」保留**直达按钮**（不依赖自动降级，防手机系统吞掉异步弹出的选择器）。
- 页面底部诊断行显示鉴权准备状态，手机上报问题时照着念即可。

### 2.8 飞书提醒运维

- **发送测试提醒**按钮（设置页 → 飞书提醒）：真发一条私聊给当前登录者，**原样显示飞书返回**（含错误码），并显示名单已认出的管理员数。收不到提醒时按显示的错误排查，不用猜。
- 常见排查顺序：① 应用功能 → **机器人**必须启用；② 权限（im:message 等）开通后必须**创建版本并发布**才生效；③ 收提醒的人在应用可用范围内；④ 名单里写姓名的，该管理员至少登录过一次应用。
- 登录名单（姓名→openId）**存多维表格设置表**（key=`knownUsers`），跨发布持久；每次有人登录自动更新。（历史上存本地文件，重新发布会被清掉导致私聊哑火，已修复。）
- 可选的群自定义机器人通道（`config.json` 的 `groupWebhook`）保留代码支持，当前按需求**关闭**（只私聊）。

### 2.9 备份与恢复（离线版）

- 整包 JSON 导出（含格式版本号）、导入前自动备份当前数据 + 二次确认 + 单事务回滚。
- 物品清单、出入库流水、兵种预算 CSV 导出。
- 超 7 天未备份提醒。

---

## 3. 数据模型

### 3.1 存储

- 离线版：IndexedDB `FEVER_INV_V1`，6 张对象表。
- 飞书版：一张多维表格 6 张表（大类 / 物品 / 采购申请 / 发票 / 出入库流水 / 设置）。
- **表结构从不动**：每行只有 主键 / 摘要 / 数据（整条 JSON）/ 更新时间 / 操作人 五列，所有业务字段都是 JSON 内部的键——加功能不加列。

### 3.2 关键行内字段（节选）

- 采购申请：`troop`（兵种，必填）、`approval`（pending/approved/rejected）、`requesterOpenId` / `requesterName`（提交时记下，批准提醒用）。
- 物品：`code`、`identityMode`、`inStockQty / lentQty / repairQty / usedUpQty / totalQty`、`purchaseRequestId`、`invoiceId`。
- 发票：`invoiceNo`（选填）、`amount`、`fileRef`（飞书版）/ `fileData`（离线版 base64）。
- 设置表：`budgetByTroop`（预算额度）、`knownUsers`（登录名单，服务端写）、`lastBackupAt`、`deleteLog`（删除留痕）。

### 3.3 并发与一致性

- 写请求走串行队列；多维表格**表间并行、表内串行**（同表并发会写冲突）。
- 自增编号前端预占、服务端校验，占用即整批 409；绝不服务端悄悄换号（会话内多表引用会指错）。
- 会话令牌为自签名无状态令牌（密钥由 appSecret 派生），进程重启 / 重新发布不踢人。

---

## 4. 架构与目录

```
v1/            界面与业务代码（离线版本体；飞书版原样复用）
  js/db.js     离线数据层（IndexedDB）
  js/zip.js    自研 ZIP 打包器（store 模式）
  js/scanner.js + lib/jsQR.js   扫码多来源降级
feishu/        飞书版专有：db-remote.js（数据层）、登录层、扫码 JSAPI 鉴权、测试
server/        Node 后端：静态伺服 + 数据代理 + 权限门 + 发票文件 + 飞书 API + 审批提醒
scripts/       构建线：build-feishu.js（白名单拼装到 dist-feishu 并同步 server/public）
docs/          FEISHU_SETUP.md（用户部署与排错手册）
v1/test/       单元测试 + 浏览器端到端（真实无头浏览器）
server/test-*  服务端测试（假 feishu / 假表格，不碰真实数据）
```

- 数据层接口两版**必须一一对应**（`db.js` ↔ `db-remote.js`），静态自检测试兜底（少一个方法就会在某一版变成 `xxx is not a function`）。
- 提醒诊断标准打法：本地用 `server/lib/session.js` 签发令牌（密钥由 appSecret 派生，两端一致）→ 打线上 `/api/notify-test` 看飞书原始错误。

---

## 5. 安全边界

- `server/config.json`（App Secret、管理员名单）只存服务端、已 gitignore、仅服务端读取；静态目录是 `server/public`，公网取不到（有实测：`/config.json`、`/../config.json` 全部落到首页兜底）。
- 未登录对 `/api/data`、`/api/write`、`/api/upload`、`/api/file/<ref>` 一律 401 且零写入（有测试钉住）。
- 管理员操作服务端强制 403；整批原子（失败整体回滚、镜像重拉）。
- 托管平台网关会覆盖 `Authorization` 头，会话令牌走自定义头 `X-Session-Token`（两处都收，逐个校验）。
- 运行时文件（登录名单、队员上传的发票 PDF）一律 gitignore，绝不进代码库。

---

## 6. 测试与发布口径

| 套件 | 数量 |
| --- | --- |
| v1 单元测试（`v1/test/test.js`） | 247 |
| v1 浏览器端到端（`v1/test/e2e/run.js`） | 142 |
| 飞书远程数据层（`feishu/test/remote-db.js`） | 80 |
| 飞书 JSAPI 鉴权（`feishu/test/feishu-jsapi.js`） | 17 |
| 飞书异步写入端到端（`feishu/test/e2e-async.js`） | 24 |
| 服务端：鉴权 / 管理员 / 提醒（`test-jsapi / test-admin / test-notify`） | 8 / 12 / 29 |
| 根主测试（`test/test.js`） | 25 |

- 改完必须做**反向验证**：把修复摘掉，确认测试真的转红。
- 发布流程：`node scripts/build-feishu.js`（同步 `server/public`）→ 托管平台覆盖发布（同 appId，链接不变）→ 线上校验（逐字节比对本地构建产物 + 新功能特征串 + 权限门冒烟）。
- 注意：改 `server/config.json` 后必须重新发布才上线（配置随应用上传）；重新发布不会清设置表里的数据与登录名单。

---

## 7. 版本历史（需求轮次）

| 轮次 | 内容 |
| --- | --- |
| 第一版 | 离线版核心：四大类、物品身份与二维码、采购到货入库、借还、流水 |
| 第二~十轮 | 离线版完善与飞书共享版上线（共享存储、免登、双层权限、冷启动与写库并行优化） |
| 第十一轮 | 批量打印标签（修 data-cat 死参数）、物品批量删除（勾选 + 单事务留痕） |
| 第十二轮 | 手机端适配 + 扫码查物（四级降级、JSAPI 毫秒级时间戳等鉴权修复） |
| 第十三轮 | 入库必填兵种；到货改传发票 PDF（桌面拖拽）；管理员批量下载发票 ZIP；飞书审批提醒 |
| 第十三轮增强 | 批量下载改为可勾选选择；兵种预算导出（三段式 CSV）；提醒诊断按钮与群机器人通道 |
| 第十四轮 | 提醒登录名单持久化到设置表（修复"发布清名单导致私聊哑火"）；按需求改为仅私聊、关闭群通道 |
