# FEver 战队物资管理 —— 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-21 |
| 适用版本 | 第一版（电脑网页版，本地离线运行） |
| 文档状态 | 需求已与需求方逐条确认，作为后续开发唯一依据 |

> 说明：本仓库原有的 index.html / js / css / test 为早期版本，需求方已确认其中**没有真实数据**。第一版将**另起一套**实现；旧文件保留不删、不再使用。

---

## 1. 产品概述

### 1.1 一句话描述

一个只在这台电脑上运行的战队物资管理网页应用：四个大类（机械 / 电控 / 视觉 / 硬件）各自有符合自身场景的功能，物品有唯一身份和二维码，采购申请到货后自动入库并留存发票，出入库登记后随时能看到每件物品现在在谁手上、什么状态。

### 1.2 运行方式与限制

- 纯本地：不联网、不登录、不用云数据库、不做服务器部署、不做手机端。
- 数据存在本机浏览器自带的数据库里（IndexedDB），刷新、关闭浏览器、重启电脑后都不丢失。
- 只面向电脑浏览器使用，界面按桌面分辨率设计。

### 1.3 使用场景

战队几个人共用一台电脑做物资登记：有人买东西要先提申请，到货后登记入库；平时借用 / 归还 / 领用东西要留痕；想快速查某个东西在不在、在谁那、有没有发票。

---

## 2. 名词定义

| 名词 | 含义 |
| --- | --- |
| 身份 | 一套「唯一编码 + 二维码 + 基本信息」的记录单位。一件物品要么独占一个身份，要么和同款共用身份。 |
| 单独建身份 | 贵重 / 数量少 / 需要单独追踪的物品，一件一个编码一个二维码，数量恒为 1。 |
| 同款共用身份 | 消耗品和普通小件，同款共用一个编码一个二维码，用「件数」表示库存。 |
| 借用 | 借出去以后要还。记录借用人、用途、预计归还日期，能查「在谁手上」「是否超期」。 |
| 领用 | 领走就用掉、不用还。直接从库存扣减件数，不跟踪归还。 |
| 在库 / 借出 / 损坏待修 | 物品当前状态，三选一（详见 6.3）。 |
| 流水 | 每一次入库、借出、归还、领用、送修、修好回库的记录，不可删除，只可追加。 |

---

## 3. 四个大类的定位（各自的场景功能）

四个大类**不是同一张表换个名字**，每个大类有自己的首页视图、自己的专属字段、自己的提醒逻辑。

### 3.1 机械（编号前缀 MC）

- 专属字段：材质、尺寸规格、承重 / 载荷、预计寿命、维护周期。
- 核心功能：
  - 按「适配车型 / 装配位置」检索零件，快速找到车上该位置用的那个件。
  - 标准件（螺丝、螺母、轴承等）走「同款共用身份 + 件数」库存模式。
  - 设置安全库存，低于安全库存时在首页和大类页高亮提示补货。

### 3.2 电控（编号前缀 EL）

- 专属字段：工作电压、工作电流、功率、接口类型（CAN / RS485 / USB 等）、安全等级。
- 核心功能：
  - 安全警示：高压、锂电池等标记为「高压危险 / 注意」，列表和详情页用醒目样式显示。
  - 易损件记录更换周期，提示到期更换。
  - 电池额外记录循环次数与健康状况。

### 3.3 视觉（编号前缀 VS）

- 专属字段：分辨率、帧率、焦距 / 视场角、镜头接口（CS / C）、标定状态、最近标定日期。
- 核心功能：
  - 相机、镜头、光源默认走「单独建身份」（一件一码）。
  - 记录镜头与相机的配套关系，能查到某个镜头配哪台相机。
  - 标定提醒：超过设定周期未标定的，标记为「需重新标定」并高亮。
  - 贵重物品借用超期重点提醒。

### 3.4 硬件（编号前缀 HW）

- 专属字段：材质、表面处理、安装孔位、装配位置、重量。
- 核心功能：
  - 工具和框架类支持借还，记录借用人与预计归还日期。
  - 耗材（螺丝、扎带、线材等）记余量，低于安全库存时报警。
  - 工具记录校准 / 检定日期，到期提示。

---

## 4. 使用流程与交互逻辑

### 4.1 页面结构

| 页面 | 作用 | 入口 |
| --- | --- | --- |
| 首页 | 四个大类卡片总览 + 最近动态 + 快捷操作 | 默认页 |
| 大类页（4 个） | 该类物品列表 + 该类专属视图（统计 / 提醒 / 筛选） | 点首页卡片 |
| 物品详情页 | 基本信息、专属字段、当前状态、二维码、完整履历、关联采购与发票 | 点物品行 |
| 出入库操作台 | 输入编号识别物品 → 选操作 → 填表 → 提交 | 顶部按钮 / 首页快捷区 |
| 采购申请列表 | 所有采购申请及状态流转 | 顶部导航 |
| 到货入库弹窗 | 填到货数量、存放位置、发票信息，一键生成物品 | 采购申请行「确认到货」 |
| 发票台账 | 所有发票，可点回对应采购申请和物品 | 顶部导航 |
| 出入库流水 | 全部操作记录，可按人 / 按物 / 按类型筛选 | 顶部导航 |
| 借用台账 | 在借清单、超期清单、归还操作 | 顶部导航 |
| 标签打印页 | 选择物品生成二维码标签，直接浏览器打印 | 物品详情 / 批量勾选 |
| 设置与数据备份 | 导出备份、导入恢复 | 顶部导航 |

全局：顶部固定导航 + 面包屑 + 返回上一页；任何列表都支持「搜索 + 筛选」。

### 4.2 主要交互流程

**流程 A｜采购 → 到货 → 自动入库**

1. 采购申请列表 →「新建申请」→ 填物品名称、规格、数量、预算、用途、申请人、所属大类 → 提交，状态为「待购买」。
2. 状态可手动推进：待购买 → 已下单 →（可取消）。
3. 货到后点「确认到货」→ 弹窗填：实际到货数量、存放位置、发票号、发票金额、供应商、发票日期。
4. 提交后系统自动：生成物品身份并入库（写入在库状态）+ 保存发票 + 写一条入库流水 + 把申请标记为「已到货」。
5. 弹窗内提供「单独建身份」开关：默认按大类规则预勾选（视觉类默认开），选择结果决定生成 1 个身份 + 件数，还是生成 N 个独立身份。

**流程 B｜直接入库（不走采购）**

出入库操作台 → 输入或生成编号 → 填名称、规格、数量、存放位置、专属字段 → 提交 → 生成身份并入库，写流水。

**流程 C｜借出与归还**

1. 操作台输入编号 → 显示该物品当前状态。
2. 选「借出」→ 填借用人、用途、预计归还日期 → 提交 → 状态变「借出」，写流水，进入借用台账。
3. 归还时输入编号 → 选「归还」→ 提交 → 状态变回「在库」，写流水，从在借清单移除。
4. 超过预计归还日期仍未归还的，在借用台账和首页用红色高亮显示「超期 N 天」。

**流程 D｜领用（耗材，不还）**

操作台输入编号 → 选「领用」→ 填件数、领用人、用途 → 提交 → 库存件数减少，写流水。件数减到 0 时状态标记为「已用完」并在列表提示补货。

**流程 E｜送修与修好**

操作台输入编号 → 选「损坏待修」→ 填故障说明 → 状态变「损坏待修」；修好后选「修好回库」→ 状态变回「在库」，写流水。

**流程 F｜查看物品身份与二维码**

物品详情 → 显示编码、二维码、名称、规格、大类、存放位置、专属字段、当前状态 → 点「打印标签」进入标签打印页；点「履历」查看该物品全部流水；点「关联采购 / 发票」跳到对应记录。

### 4.3 交互一致性约定

- 状态类操作（借出、归还、领用、送修、修好）全部走同一个操作台，先识别物品、再选动作，避免各处入口写法不一。
- 每个动作提交前都要展示「本次操作会带来的变化」（例如：库存 20 → 14，状态 在库 → 借出），确认后才生效。
- 所有写操作成功后给出轻提示，并立即刷新列表与首页统计。

---

## 5. 模块之间的关联

数据不是各存各的，核心几张表通过编码互相挂接，任意一条记录都能点回它的上下游。

```
采购申请 ──1:1──> 发票
    │                │
    │ 到货自动生成    │ 记录来源
    v                v
   物品身份 <─────────┘
    │
    │ 每次出入库追加
    v
 出入库流水 ──(借出 / 归还)──> 借用台账
```

- **采购申请 ↔ 发票**：一张申请对应一张到货发票，发票上有「来源申请」链接。
- **采购申请 ↔ 物品**：物品上保存来源申请编号；点物品能看到「这东西是哪个申请买回来的、花了多少、谁申请的」。
- **发票 ↔ 物品**：发票详情能看到本次到货生成了哪些物品身份。
- **物品 ↔ 流水**：物品详情的时间线就是它的全部流水，按时间倒序。
- **流水 ↔ 借用台账**：借用台账不是独立数据，而是从流水里推导出的「当前仍处于借出状态」的清单；归还后自动消失。
- **大类 ↔ 全部**：每个大类页的统计（数量、在借、待修、低库存、待购买）都从上述数据实时算出，不做重复存储，避免两边不一致。

关联断裂的处理：当某条上游记录被删除（例如误建采购申请），下游只做「来源已删除」的提示，不阻断显示，保证历史流水完整可查。

---

## 6. 物品信息清单、编码与二维码

### 6.1 物品基础信息（所有大类通用）

| 字段 | 是否必填 | 说明 |
| --- | --- | --- |
| 编码 | 是（系统生成） | 唯一身份标识，见 6.2 |
| 名称 | 是 | 例如 步进电机 NEMA17 |
| 规格 / 型号 | 否 | 例如 42BYGH 1.5A |
| 所属大类 | 是 | 机械 / 电控 / 视觉 / 硬件 |
| 存放位置 | 否 | 例如 A 区 3 号柜、车架下层收纳盒 |
| 件数 | 是 | 单独建身份固定为 1；同款共用身份为实际库存数 |
| 安全库存 | 否 | 低于此值触发补货提醒 |
| 备注 | 否 | 自由填写 |
| 专属字段 | 否 | 按大类不同，见第 3 节 |
| 当前状态 | 是（系统维护） | 在库 / 借出 / 损坏待修 |
| 来源采购申请 | 否（系统写入） | 采购到货自动入库时写入 |
| 来源发票 | 否（系统写入） | 采购到货自动入库时写入 |
| 创建时间 | 是（系统写入） | 入库时间 |

### 6.2 编码规则

- 格式：大类前缀 - 四位流水号，例如 MC-0001、EL-0007、VS-0003、HW-0012。
- 前缀：机械 MC、电控 EL、视觉 VS、硬件 HW。
- 流水号按大类独立递增，不复用、不回收，确保历史流水里的编码永远能查到东西。
- 一个编码 = 一个身份。同款共用身份时，件数写在物品上；单独建身份时，入库几件就生成几个连续编码。
- 编码一经生成不可修改（避免贴好的标签失效）。

### 6.3 状态规则

| 状态 | 含义 | 进入方式 | 离开方式 |
| --- | --- | --- | --- |
| 在库 | 可用、可借 | 入库、归还、修好回库 | 借出 / 领用 / 送修 |
| 借出 | 在别人手上，需归还 | 借出操作 | 归还操作 |
| 损坏待修 | 故障或损坏，不能使用 | 送修操作 | 修好回库 |

同款共用身份的物品按件数细分：总件数、在库件数、借出件数、待修件数，四个数字始终自洽（在库 + 借出 + 待修 = 总数）。件数减到 0 时状态额外标记「已用完」。

### 6.4 二维码生成

- 每个身份生成一个二维码，二维码内容为固定格式的纯文本：FEVER:ITEM:编码，例如 FEVER:ITEM:VS-0003。
  - 采用纯文本而非网址：第一版不联网，扫码内容必须是本机可解析的字符串；第二版手机 App 按同样格式解析即可。
- 二维码在本地由脚本生成，**不联网、不调用任何外部服务**。
- 物品详情页可查看大图二维码；标签打印页可生成可打印的标签。
- 标签内容：二维码 + 编码 + 名称 + 规格 + 大类 + 存放位置（文字，方便肉眼核对）。
- 打印支持常见不干胶标签尺寸，并支持一次选择多个物品批量排版打印。

---

## 7. 本地数据：保存、备份与恢复

### 7.1 存储方式

- 使用浏览器自带的 IndexedDB（本机数据库），无需安装软件、无需联网。
- 数据按「浏览器 + 打开方式」隔离：同一台电脑、同一个浏览器、同一种打开方式，数据才互通。

### 7.2 打开方式（开发阶段必须实测后二选一，并固定下来）

| 方案 | 说明 | 取舍 |
| --- | --- | --- |
| A（优先）双击 index.html | 零依赖，最像「打开一个网页」 | 需实测目标浏览器在本地文件方式下 IndexedDB 可用 |
| B 双击 启动.bat | 用本机已安装的 Node 起一个本地静态服务并自动打开浏览器，完全离线 | 多一个启动文件，但存储稳定性最好 |

**约定：最终只保留一种方式，并在 README 中写明。** 两种方式会产生两套互不相通的数据，绝不能混用。

### 7.3 数据不丢的保证

- 刷新页面、关闭标签页、关闭浏览器、重启电脑，数据仍在。
- 所有写操作立即落库，不存在「没点保存就丢」的情况。

### 7.4 备份与恢复

- **导出备份**：设置页点「导出备份」，生成一个 .json 文件（包含物品、采购申请、发票、流水、大类配置，以及备份格式版本号和导出时间），保存到电脑任意位置。
- **导入恢复**：选择备份文件导入，恢复全部数据。
  - 导入前必须先自动导出当前数据一份作为保险，并二次确认，防止误操作覆盖。
  - 导入完成后给出结果摘要（恢复了多少条物品 / 申请 / 发票 / 流水）。
- **提醒**：设置页显示「上次备份时间」，超过 7 天未备份则提示一次。

### 7.5 已知风险（必须让使用者知情）

- 手动清除浏览器数据、换一个浏览器、换 Windows 用户、使用无痕窗口，数据会看不到或丢失。
- 因此：重要节点前后请导出备份，建议每周至少导出一次，备份文件存到网盘或 U 盘。
- 第一版不做自动多端同步（明确不做联网），这是刻意的取舍。

---

## 8. 第一版必须完成的功能（范围锁定）

1. 四个大类首页卡片，每张卡片显示该类关键指标（数量、在借、待修、低库存、待购买等）。
2. 四个大类各自的专属页面与专属字段，按第 3 节实现，功能互不相同。
3. 物品身份：支持「单独建身份」与「同款共用身份」两种模式，支持编码自动生成。
4. 物品二维码：本地生成、详情页可看、可打印标签、支持批量打印排版。
5. 出入库操作台：输入编号即可识别物品并执行 借出 / 归还 / 领用 / 送修 / 修好回库，并记录操作人、时间、用途。
6. 采购流程：新建申请 → 状态流转（待购买 / 已下单 / 已到货 / 已取消）→ 确认到货。
7. 到货自动入库：填完到货数量、存放位置、发票信息后，自动生成物品身份并入库，同时保存发票、写流水。
8. 发票信息：发票号、金额、供应商、日期，并与采购申请、物品互相跳转。
9. 物品详情页：基本信息 + 专属字段 + 当前状态 + 二维码 + 完整履历时间线 + 关联采购与发票。
10. 借用台账：在借清单、超期高亮、一键归还。
11. 出入库流水页：全部记录可查、可筛选（按物品 / 按操作人 / 按类型 / 按时间）。
12. 低库存与到期提醒：安全库存、标定到期、工具校准到期、易损件更换到期。
13. 数据备份与恢复：导出 .json、导入恢复、上次备份时间提醒。
14. 数据持久化：刷新 / 关浏览器 / 重启电脑后数据完整保留。
15. 本地离线运行：不联网、不登录、无云数据库、无手机端界面。

---

## 9. 第一版明确不做的功能

| 不做的事 | 说明 |
| --- | --- |
| 手机端界面 / 手机 App | 第二版做，第一版只按电脑屏幕设计 |
| 摄像头扫码 | 第一版不调用摄像头；出入库靠输入编号（兼容 USB 扫码枪，因为它等同键盘输入） |
| 登录、账号、多用户权限 | 操作时手动填「操作人」即可 |
| 联网、云数据库、服务器部署 | 完全本地运行 |
| 多级审批流 | 采购申请只做状态流转，不做审批人签核 |
| 消息通知 / 待办推送 | 不做 |
| 财务统计报表、Excel 导出 | 不做，看流水和台账即可 |
| 报废处理流程 | 不做（只有 损坏待修，报废后续再说） |
| 物品照片 | 不做（第一版不涉及图片上传） |
| 盘点功能 | 不做 |

**为第二版预留（第一版不实现，但数据结构上留好位置）**：手机端界面、摄像头扫码、物品照片、报废流程、Excel 导出。

---

## 10. 验收标准（可实际检查）

以下每条都可以由使用者在电脑上手动操作验证。

### 10.1 运行与持久化

- AC-01 双击打开应用，无需联网即可完整加载出首页，浏览器控制台无报错。
- AC-02 录入一条物品后按 F5 刷新页面，该物品仍在列表中，内容一致。
- AC-03 录入物品后完全关闭浏览器再重新打开，物品仍在。
- AC-04 重启电脑后重新打开，物品、采购申请、发票、流水全部仍在。

### 10.2 四大类

- AC-05 首页显示且仅显示四个大类卡片：机械、电控、视觉、硬件，每张卡片的指标数字与类内实际数据一致。
- AC-06 四类页面功能明显不同：机械能按「适配车型 / 装配位置」筛选并显示安全库存提示；电控显示安全等级警示；视觉显示标定状态与标定提醒；硬件显示校准日期与耗材余量。
- AC-07 在某类新增物品时，只出现该类专属字段（例如机械出现材质、尺寸；视觉出现分辨率、镜头接口）。

### 10.3 身份与二维码

- AC-08 入库 1 件并选「单独建身份」，生成 1 个编码 + 1 个二维码；入库 5 件同款并选「共用身份」，只生成 1 个编码，件数为 5。
- AC-09 连续新增同类物品，编码按 MC-0001、MC-0002 顺延，不重复、不复用。
- AC-10 物品详情页能显示二维码；标签打印页打印出的标签上二维码清晰可扫，且同时含编码、名称、规格、大类、位置文字。
- AC-11 二维码内容为 FEVER:ITEM:编码 格式（用任意扫码工具扫出的文本可核对）。

### 10.4 采购与到货入库

- AC-12 新建采购申请（物品、数量、预算、用途、申请人、大类齐全），提交后出现在列表中，状态为「待购买」。
- AC-13 申请状态可依次推进为「已下单」「已到货」，也可取消。
- AC-14 点「确认到货」，填数量、存放位置、发票号、金额、供应商、日期后提交，物品自动出现在对应大类中且状态为「在库」。
- AC-15 该物品详情里能看到来源采购申请与发票信息；发票台账里能看到这张发票，并能点回申请和物品。
- AC-16 必填项（如发票号、供应商）留空时无法提交，并给出明确提示。

### 10.5 出入库与借用

- AC-17 在操作台输入某个编码，能正确识别物品并显示其名称与当前状态。
- AC-18 输入不存在的编码，给出「未找到该物品」提示，不产生任何记录。
- AC-19 借出时填写借用人、用途、预计归还日期后，物品状态变为「借出」，借用台账出现该条记录，流水新增一条。
- AC-20 归还后状态变回「在库」，借用台账中该条消失，流水新增一条归还记录。
- AC-21 把预计归还日期设为昨天，借用台账中该条显示「超期 1 天」并使用醒目颜色。
- AC-22 领用 3 件同款物品，件数从 N 减为 N-3，流水记录件数与领用人；领用到 0 时出现补货提示。
- AC-23 送修后状态为「损坏待修」，修好回库后状态回到「在库」，两次操作都有流水。
- AC-24 同款物品的「在库 + 借出 + 待修」始终等于总件数，任何一次操作后都成立。

### 10.6 关联与流水

- AC-25 任意一条流水都能点回对应的物品；任意物品都能看到它的全部流水，按时间倒序。
- AC-26 流水可按物品、操作人、操作类型、时间范围筛选，结果正确。
- AC-27 删除一条采购申请后，已生成的物品与历史流水仍完整可查，来源处显示「来源已删除」。

### 10.7 提醒

- AC-28 把某物品件数降到低于安全库存，首页与大类页出现补货提示。
- AC-29 视觉类物品标定日期超过设定周期，显示「需重新标定」高亮。
- AC-30 硬件类工具校准日期到期，列表中出现到期提示。

### 10.8 备份与恢复

- AC-31 点「导出备份」生成 .json 文件，内容包含物品、申请、发票、流水、大类配置及版本号。
- AC-32 清空数据后导入该备份文件，物品、申请、发票、流水数量与导出前完全一致。
- AC-33 导入前会先自动导出当前数据并二次确认，用户取消则不发生任何改变。
- AC-34 超过 7 天未备份，设置页出现备份提醒。

### 10.9 范围检查

- AC-35 全站无登录、无联网请求、无手机端界面元素、无摄像头调用。

---

## 11. 技术约定与工程约束

- 技术栈：纯前端 HTML + CSS + 原生 JavaScript + IndexedDB，不引入需要联网的依赖；二维码生成脚本与库文件全部放在本地 js/lib。
- 目录规划（拟）：
  - index.html 页面骨架
  - css/ 样式
  - js/db.js 数据层（表结构、读写、备份导出导入）
  - js/modules/ 四个大类的专属逻辑
  - js/app.js 界面与交互
  - js/lib/ 本地库（二维码等）
  - test/ 用 Node + fake-indexeddb 跑数据层与业务规则的自测
- 旧版本文件不删除、不再引用。
- 开发过程中需按第 10 节逐条自查；数据层逻辑用 npm test 自动验证，界面部分人工验证。

---

## 12. 待第二版讨论（不在第一版范围）

- 手机 App 端界面与摄像头扫码（按 FEVER:ITEM:编码 格式识别）。
- 物品照片。
- 报废处理流程。
- Excel / 表格导出。
- 采购审批流与消息提醒。
- 多端数据同步（如需要，届时再评估脱离「纯本机」的方案）。

---

## 13. 需求确认记录

| 编号 | 问题 | 结论 |
| --- | --- | --- |
| Q1 | 物品身份按「每一件」还是「每一款」 | 混合：贵重且数量少的单独建身份（一件一码），其余同款共用身份 + 件数 |
| Q2 | 出库是「借出去要还」还是「领走不还」 | 两者都要：保留「在谁手上、该还了没」的借用能力，耗材走领用扣减 |
| Q3 | 旧版本里的数据是否保留 | 无真实数据，直接另起一套，旧文件保留不删 |
| Q4 | 用什么设备扫码 | 计划第二版用手机摄像头；第一版只做电脑页面，不接摄像头，用输入编号代替 |
| Q5 | 四大类设计是否可行、是否保留「损坏待修」状态 | 可行，保留「损坏待修」 |

---

## 14. 文档变更记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v1.0 | 2026-09-21 | 首版，依据 Q1-Q5 确认结论编写，作为第一版开发依据 |
